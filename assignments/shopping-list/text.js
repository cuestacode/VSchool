const form = document["label"]

form.addEventListener("submit", function (event){
    event.preventDefault()

    alert("It works")

    const label = label.food.value
    label.food.value = ""

    const li = document.createElement ("li")
    li.textContent = food
    document.getElementsByTagName ("ul")[0].append
    (li)

    const list = document["list"]
    const li1 = document.createElemnent ("button")
    li1.button = edit 
    document.getElementsByTagName("ul")[0].append
    (li1)
}
)